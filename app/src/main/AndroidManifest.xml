<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.questionanswerforum">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.FirstScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.RegistrationActivity"></activity>
        <activity android:name=".activities.QuestAnsForumActivity"></activity>
        <activity android:name=".activities.PostQuestionActivity"></activity>
        <activity android:name=".activities.ListOfQuestionsActivity"></activity>
        <activity android:name=".activities.GiveAnswerActivity"></activity>
        <activity android:name=".activities.GetAnswersActivity"></activity>
        <activity android:name=".activities.MyAnswersActivity"></activity>

        <activity android:name=".activities.TeacherLoginActivity"></activity>
        <activity android:name=".activities.StudentLoginActivity"></activity>
        <activity android:name=".activities.AdminLoginActivity"></activity>
        <activity android:name=".activities.StudentDashBoardActivity"></activity>
        <activity android:name=".activities.AddCommentActivity"></activity>
        <activity android:name=".activities.ViewCommentActivity"></activity>
        <activity android:name=".activities.TeacherDashBoardActivity"></activity>
        <activity android:name=".activities.AdminDashBoardActivity"></activity>




    </application>

</manifest>